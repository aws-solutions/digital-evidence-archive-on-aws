{
    "name": "cdk-solution",
    "version": "0.1.0",
    "bin": {
        "cdk-solution": "bin/cdk-solution.js"
    },
    "scripts": {
        "cleanup": "tsc --build ./ --clean && rm -rf node_modules && rm -f package-lock.json",
        "cleanup:tsc": "tsc --build ./ --clean",
        "build": "tsc",
        "watch": "tsc -w",
        "test": "jest --coverage",
        "cdk": "cdk"
    },
    "peerDependencies": {
        "constructs": "^10.0.0"
    },
    "devDependencies": {
        "@types/jest": "^27.0.0",
        "@types/node": "^16.4.0",
        "@types/sinon": "^10.0.2",
        "aws-cdk": "%%CDK_VERSION%%",
        "jest": "^27.0.6",
        "aws-cdk-lib": "2.26.0",
        "sinon": "^11.1.1",
        "ts-jest": "^27.1.5",
        "ts-node": "^10.1.0",
        "typescript": "^4.3.5"
    },
    "dependencies": {
        "@aws-solutions-constructs/aws-apigateway-lambda": "^2.8.0",
        "jest-cdk-snapshot": "^2.0.1",
        "source-map-support": "^0.5.16"
    }
}
